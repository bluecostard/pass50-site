name: Metrics Priority P0

on:
  schedule:
    - cron: '*/15 * * * *'
  workflow_dispatch:

concurrency:
  group: pass50-metrics-p0
  cancel-in-progress: false

jobs:
  collect:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    env:
      CRON_URL: ${{ secrets.PASS50_METRICS_CRON_URL }}
      CRON_SECRET: ${{ secrets.PASS50_METRICS_CRON_SECRET }}
    steps:
      - name: Dispatch and process P0
        shell: bash
        run: |
          if [ -z "$CRON_URL" ] || [ -z "$CRON_SECRET" ]; then
            echo "::warning::PASS50 metrics cron secrets are not configured."
            exit 0
          fi
          dispatch_id="${GITHUB_RUN_ID}-${GITHUB_RUN_ATTEMPT}-p0"
          call_cron() {
            local body="$1" timestamp signature
            timestamp="$(date +%s)"
            signature="$(printf '%s\n%s' "$timestamp" "$body" | openssl dgst -sha256 -hmac "$CRON_SECRET" -r | awk '{print $1}')"
            curl --fail-with-body --silent --show-error --max-time 55 \
              -H "Content-Type: application/json" \
              -H "X-P50-Timestamp: $timestamp" \
              -H "X-P50-Signature: $signature" \
              --data "$body" "$CRON_URL"
          }
          response="$(call_cron "$(jq -nc --arg id "$dispatch_id" '{action:"dispatch",cadence:"p0",dispatchId:$id}')")"
          remaining="$(jq -r '.remaining // 0' <<<"$response")"
          processed=0
          for iteration in $(seq 1 40); do
            [ "$remaining" -eq 0 ] && break
            response="$(call_cron "$(jq -nc --arg id "$dispatch_id" --argjson iteration "$iteration" '{action:"work",cadence:"p0",dispatchId:$id,iteration:$iteration}')")"
            remaining="$(jq -r '.remaining // 0' <<<"$response")"
            processed=$((processed + $(jq -r '.processed // 0' <<<"$response")))
          done
          {
            echo "### Metrics Priority P0"
            echo "- Dispatch: \`$dispatch_id\`"
            echo "- Tâches traitées: $processed"
            echo "- File restante: $remaining"
          } >> "$GITHUB_STEP_SUMMARY"
