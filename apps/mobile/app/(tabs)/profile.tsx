import { SiteWebView } from '@/components/SiteWebView';

/** Mon espace — page dédiée mon-espace.html. */
export default function ProfileScreen() {
  return <SiteWebView tab="account" title="MON ESPACE" />;
}
