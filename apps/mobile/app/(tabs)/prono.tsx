import { SiteWebView } from '@/components/SiteWebView';

/** Pronos — pronostics.html (sans redirection vers le Classement). */
export default function PronoScreen() {
  return <SiteWebView tab="prono" title="PRONOS" />;
}
